#include <stdio.h>
int main(int argc, char *argv[])
{   // input 2 valori limite if(compresi fra 30 e 50) --> output valori; input 5 int in "n",  print somma if (n > limite1 && n < limite 2)
    int limite1, limite2, n;


    return 0;
}